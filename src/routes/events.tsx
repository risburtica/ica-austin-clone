import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { EventCard, PageIntro, SectionTitle } from "@/components/page-parts";
import { events, pageHead, pastEvents } from "@/lib/site-data";
export const Route = createFileRoute("/events")({ head: () => pageHead("Events Calendar", "Upcoming and past ICA community events in Central Texas.", "/events"), component: Events });
function Events(){const [selected,setSelected]=useState(7);const first=events[0]!;const weeks=[...Array(35)].map((_,i)=>i<6?null:i-5);return <><PageIntro eyebrow="Gather with us" title="Events Calendar"><p>From Sunday brunches and Christmas parties to fundraisers and service days — there's always a way to join in.</p></PageIntro><section className="site-container grid gap-10 py-14 lg:grid-cols-[1.1fr_.9fr]"><div className="warm-card p-5 sm:p-7"><div className="flex items-center justify-between"><h2 className="font-display text-2xl font-semibold text-primary">November 2026</h2><span className="tag">Today</span></div><div className="mt-6 grid grid-cols-7 text-center text-xs font-semibold text-muted-foreground">{"SMTWTFS".split("").map((x,i)=><div key={i}>{x}</div>)}</div><div className="mt-2 grid grid-cols-7 gap-1">{weeks.map((d,i)=><button key={i} disabled={!d} onClick={()=>d&&setSelected(d)} className={`aspect-square rounded-md text-sm ${d===7?"bg-primary font-bold text-primary-foreground":d?"hover:bg-muted":""}`}>{d}</button>)}</div><p className="mt-5 text-center text-xs text-muted-foreground">Highlighted days have events — tap one to see details.</p></div><div><SectionTitle title="Selected date"/><div className="mt-5"><EventCard {...first} date={selected===7?`${first.date} · ${first.time}`:"No event scheduled"}/></div></div></section><section className="site-container py-10"><SectionTitle title="Upcoming"/><div className="mt-7 grid gap-5 lg:grid-cols-3">{events.map((e,i)=>i===0?<Link key={e.title} to="/rsvp" className="group block" aria-label="RSVP for the Annual Membership Meeting"><div className="relative"><EventCard {...e} date={`${e.date} · ${e.time}`}/><span className="absolute right-4 bottom-4 rounded-full bg-accent px-4 py-1.5 text-sm font-semibold text-accent-foreground transition group-hover:scale-105">RSVP →</span></div></Link>:<EventCard key={e.title} {...e} date={`${e.date} · ${e.time}`}/>)}</div></section><section className="site-container py-14"><SectionTitle title="Past Events"/><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{pastEvents.map(e=><EventCard key={e.title} title={e.title} date={e.date} place={e.place} tags={e.tags.split(" · ")}/>)}</div></section></>}